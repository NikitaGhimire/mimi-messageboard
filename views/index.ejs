<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=title%></title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <h1><%= title %></h1>
    <ul>
        <% if (messages && messages.length > 0) { %>
            <% messages.forEach((message) => { %>
                <li>
                    <strong><%= message.user %></strong>:
                    <%= message.text %> - <%= message.added.toLocaleString() %>
                    <form action="/message/<%= message.id %>/delete" method="POST">
                        <button type="submit" onclick="return confirm('Are you sure you want to delete this message?')">Delete</button>
                    </form>
                </li>
            <% }) %>
        <% } else { %>
            <li>No messages found.</li>
        <% } %>
    </ul>
    <div class="container">
        <a href="/message/new"> Add a new message   </a>
    </div>
    
</body>
</html>